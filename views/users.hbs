<h1>Users</h1>
{{#each users as |user|}}
<h2>{{user.name}}: </h2>
<ul>
    {{#each user.platforms as |platform|}}
    <li>{{platform.platformName}}: {{platform.userName}}</li>
    {{/each}}
</ul>
{{/each}}